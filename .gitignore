*.bin
!tests/fixtures/*.bin
*.o
*.a
*.so
*.dSYM/
.DS_Store
surge-info
surge-ref
surge
surge-bench
# M2 gate run artifacts (regenerated by the commands in todo.md; the logit
# dumps alone are 126 MB per prompt per path)
m2out/
tests/fixtures/m1/*.full.f32
tests/fixtures/m1/dumps.json
# M3.4 gate (A) full logit dumps: ~64 MB per path; only the digest is committed
tests/fixtures/m3q8/*.full.f32
# Metal build artifacts (rebuilt from the src/*.metal sources by `make`; the
# two .air files are linked into the one metallib)
src/kernels.air
src/kernels_splitk.air
src/kernels.metallib
__pycache__/
